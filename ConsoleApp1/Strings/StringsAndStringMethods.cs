namespace ConsoleApp1;

public class Strings1
{
    private static string mySentence = "Look you massive bell end and now I am going to slap you in the face";

    public static void Application()
    {
        // var myString = SummarizeText.Summarize(mySentence, 10);
        // Console.WriteLine(myString);
        
        // StrBuild.buildDem();
        
        Excercizes.app();
    }
}